/** @jsxImportSource @emotion/react */
import { SigninInputWrapper, SignInTitle } from "./signin.style";

const SignInMent = () => {
  return (
    <div css={SigninInputWrapper}>
      <p css={SignInTitle}>회원가입</p>
    </div>
  );
};

export default SignInMent;